# Rapport CHECK — Audit initial VM cloud
**Timestamp** : 2026-07-28 18:10
**Cible** : 80.251.100.175 (pbx1-procom.dyndns.org) — Debian 13
**Agent** : CHECK (cycle #1 — audit de reconnaissance)

---

## STATUT : VM VIERGE CÔTÉ PBX — SÉCURITÉ DE BASE OK

---

## RÉSULTATS

### Connectivité
| Point | Résultat |
|-------|----------|
| Port 22 (SSH) | ✅ ouvert et fonctionnel |
| Port 5060 (SIP) | ❌ filtré (non ouvert — attendu à ce stade) |
| Port 8888 (provisioning) | ❌ filtré (non ouvert — attendu) |
| Port 8090 (panel) | ❌ non ouvert (attendu) |
| DNS pbx1-procom.dyndns.org | ✅ résout vers 80.251.100.175 (confirmé par pilote_status) |

### Accès SSH
- **Méthode** : `sshpass` + mot de passe fonctionne sur cette VM (contrairement au PBX01 local via tunnel PTY)
- **Compte** : `procom` (sudo avec mot de passe requis — pas de NOPASSWD)
- **Note pour OPS** : `sudo -S` + mot de passe en pipe stdin fonctionne

### Système
| Élément | Valeur |
|---------|--------|
| OS | Debian 13 — kernel 6.12.90+deb13-amd64 |
| RAM | 7,8 Go total / 7,4 Go libre |
| Disque | 67 Go total / 65 Go libre (2 Go utilisés) |
| Architecture | x86_64 |

### Services téléphonie (stack PBX)
| Service | État |
|---------|------|
| asterisk | ❌ non installé |
| kamailio | ❌ non installé |
| rtpengine | ❌ non installé |
| nginx | ❌ non installé |
| docker | ❌ absent |
| pbx-panel | ❌ non installé |

**La VM est vierge côté PBX — aucun service téléphonie en place.**

### Sécurité — nftables
Configuration restrictive en place :
```
table ip filter {
  chain mainchain {
    policy drop;
    ct state { established, related } accept
    iif "lo" accept
    ip protocol icmp accept
    ip saddr 86.219.153.123  tcp dport 22 accept   ← IP bureau ?
    ip saddr 217.181.224.153 tcp dport 22 accept   ← IP Procom/admin
    ip saddr 195.135.34.181  tcp dport 22 accept   ← IP bureau ?
  }
}
```
- **Politique par défaut : DROP** ✅
- **SSH en liste blanche (3 IPs)** ✅ — seules ces IPs peuvent se connecter
- Aucun port SIP/RTP/prov exposé pour l'instant ✅

### Sécurité — fail2ban
- **Actif** ✅ — jail `sshd` en place
- Aucune autre jail configurée (SIP absente car Kamailio non installé)

---

## ANOMALIES

1. **Stack PBX absente** — normal pour une VM fraîche, mais à signaler : aucun test fonctionnel SIP/appels/provisioning ne peut être fait avant déploiement.
2. **sudo sans NOPASSWD** — complique les scripts automatisés. `sshpass` + `sudo -S` fonctionnel mais moins propre. À prévoir pour les scripts de déploiement OPS.
3. **Pas de clé SSH publique** configurée pour le compte `procom` (accès uniquement par mot de passe). Recommandation : déployer une clé pour sécuriser et simplifier l'automatisation.
4. **Notre IP non autorisée en SSH** — depuis ce poste (julien@30.150), nous ne sommes pas dans la whitelist nftables. L'accès actuel fonctionne parce que `sshpass` passe par l'IP publique du poste 217.181.224.153 (qui est dans la whitelist).

---

## SUITE

**Attente** : dès que OPS déploie la stack (Kamailio, Asterisk 21, rtpengine) et déclare ready, CHECK effectuera :
1. Test d'enregistrement SIP (extensions 101/102/103)
2. Test appel interne (101 → 102)
3. Test autoprovisioning Yealink (fichier MAC.cfg sur nginx:8888)
4. Validation fail2ban SIP (jail sip-kamailio ou équivalent)
5. Audit nftables post-déploiement (vérifier ouverture 5060/8888 correctement filtrée)
6. Test trunk opérateur (si configuré)

**Grille de validation disponible** sur demande via check_task_*.md.

**Monitoring actif** : ce dossier est surveillé toutes les 60 secondes pour check_task_*.md.
